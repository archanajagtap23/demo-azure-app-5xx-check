# Incident Report — Azure App Service Auto-Rollback Executed

**Date:** 2026-06-17T09:51:29.429Z
**Alert:** demo-azure-5xx-5xx-alert
**App Service:** demo-azure-5xx-appln (https://demo-azure-5xx-appln.azurewebsites.net)
**Resource Group:** demo-azure-5xx-rg

## What Happened

A new deployment to the Azure App Service triggered a surge in HTTP 5xx errors, causing the Azure Monitor alert `demo-azure-5xx-5xx-alert` to fire. The Aziron Azure Incident Validation Flow was automatically invoked, performed health checks, fetched Azure Monitor metrics, and executed integration tests — all of which confirmed a critical failure in the newly deployed version. An automatic rollback to the stable v1 zip was initiated and completed successfully.

## Root Cause

The root cause was identified as a broken integration between the `/checkout` route and the `cart_service.get_cart()` function in `app.py`. The newly deployed version introduced a regression that caused unhandled exceptions in the checkout endpoint, resulting in HTTP 500 responses for all requests hitting that route.

## Azure Monitor Evidence

- Total Http5xx errors: 27
- Error rate: 94%
- Affected endpoints: `/checkout`, `/cart`

## Auto-Remediation

Aziron detected the failure and automatically redeployed the stable v1 zip to the Azure App Service.
Post-rollback verification: PASSED — all endpoints restored to HTTP 200.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout route (`app.py`)
- Ensure all 3 integration tests pass locally before deploying
- Test locally: `python3 tests/integration_test.py http://localhost:8000`
- Deploy: `.\infra\deploy.ps1 deploy`  (or `./infra/deploy.sh deploy` on Linux/Mac)

---
*Auto-generated by Aziron Azure Incident Validation Flow*
